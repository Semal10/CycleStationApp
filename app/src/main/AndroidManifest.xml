<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cyclestationsemal">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Admin.user.BookCyclePayment"></activity>
        <activity android:name=".Admin.user.BookCycleOtp" />
        <activity android:name=".Admin.user.BookCycleCard" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".StationMapsActivity"
            android:label="@string/title_activity_station_maps" />
        <activity android:name=".UserHomeScreen" />
        <activity android:name=".Admin.station.AdminDeleteStation" />
        <activity android:name=".Admin.cycle.AdminDeleteCycle" />
        <activity android:name=".Admin.station.AdminUpdateStation" />
        <activity android:name=".Admin.user.AdminDeleteUser" />
        <activity android:name=".Admin.cycle.AdminUpdateCycle" />
        <activity android:name=".Admin.user.AdminUpdateUser" />
        <activity android:name=".Admin.station.StationCustomListView" />
        <activity android:name=".Admin.cycle.CycleCustomListView" />
        <activity android:name=".Admin.user.UserCustomListView" />
        <activity android:name=".Admin.station.AdminStationView" />
        <activity android:name=".Admin.cycle.AdminCycleView" />
        <activity android:name=".Admin.station.InsertNewStation" />
        <activity android:name=".Admin.cycle.InsertNewCycle" />
        <activity android:name=".Admin.station.AdminStationHome" />
        <activity android:name=".Admin.cycle.AdminCycleHome" />
        <activity android:name=".Admin.user.AdminUserView" />
        <activity android:name=".Admin.AdminHome" />
        <activity android:name=".Otp" />
        <activity android:name=".Admin.user.AdminUserHome" />
        <activity android:name=".Admin.user.InsertNewUser" />
        <activity android:name=".RegisterNewUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>